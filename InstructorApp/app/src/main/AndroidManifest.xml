<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.azeem.instructorapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity
            android:name=".androidCore.activites.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/ToolBarTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".androidCore.activites.CourseDashboardActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/ToolBarTheme">
        </activity>

        <activity
            android:name=".androidCore.activites.AuthenticationActivity"
            android:theme="@style/ToolBarTheme">
        </activity>


        <activity
            android:name=".androidCore.activites.CoursesListActivity"
            android:theme="@style/ToolBarTheme">
        </activity>

        <activity
            android:name=".androidCore.activites.AttendanceActivity"
            android:theme="@style/ToolBarTheme">
        </activity>

    </application>
</manifest>
